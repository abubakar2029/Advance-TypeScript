// To represent a list of related constants

const small = 1;
const medium = 2;
const large = 3;

// PascalCase
const enum Size {
  Small,
  Medium,
  Large,
}
// By default values will be assigned starting from 0

let mySize: Size = Size.Medium;

// In most cases we set Explicit Values
 